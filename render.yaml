services:
  - type: web
    name: xcodeproj-to-ipa
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 300 --workers 1 --threads 4
    envVars:
      - key: SECRET_KEY
        generateValue: true
